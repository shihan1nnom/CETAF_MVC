@model Version_4.Models.Sede
@{
    ViewBag.Title = "Crear";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div style="overflow:auto">
    <div class="row-cols-1">
        <div class="col">
            <div class="card">
                <div class="card-header text-center bg-dark align-middle" style="color:white; text-shadow:black">
                    <h6 style="font:900">CREAR NUEVA SEDE</h6>
                </div>
                <div class="card-body" style="align-self:center">
                    @using (Html.BeginForm("Crear", "Sedes", FormMethod.Post))
                    {
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                        <div class="form-group">
                            @Html.Label("Nombre de la sede:")
                            @Html.TextBoxFor(m => m.Nombre, "", new { @class = "form-control", @placeholder = "Nombre" })
                            @Html.ValidationMessageFor(m => m.Nombre, "", new { @class = "badge badge-danger" })
                            @if (ViewBag.Error != null)
                            {
                                <span class="badge badge-danger">@ViewBag.Error</span>
                            }
                        </div>
                        <div class="form-group">
                            @Html.Label("Ciudad:")
                            @Html.TextBoxFor(m => m.Ciudad, "", new { @class = "form-control", @placeholder = "Ciudad" })
                            @Html.ValidationMessageFor(m => m.Ciudad, "", new { @class = "badge badge-danger" })
                        </div>
                        <div class="form-group justify-content-md-center">
                            <input type="submit" name="submit" class="btn btn-outline-primary" value="REGISTRAR" style="width: 300px" />
                        </div>
                    }
                </div>
                <div class="row" style="float:left; margin-left:100px; margin-bottom:30px">
                    <a href="@Url.Action("Index","Sedes")" style="margin-right:15px; width:25px; height:25px; margin-left:15px;" class="icon icon-return" data-toggle="tooltip" data-placement="top" title="Regresar a Lista"></a>
                </div>
            </div>
        </div>
    </div>
</div>

